name: Deploy API

on:
  workflow_dispatch:

env:
  API_IMAGE_NAME: ${{ github.repository }}/api

jobs:
  deploy-api:
    runs-on: self-hosted

    steps:
      - name: Set version from git hash
        id: version
        run: |
          echo "version=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Copy env file
        run: |
          SOURCE=~/envs/turborepo-starter/apps/api/.env.production
          DEST=./apps/api/.env.production

          if [ ! -f "$SOURCE" ]; then
            echo "❌ Source env file not found: $SOURCE"
            exit 1
          fi

          cp "$SOURCE" "$DEST"

          if [ ! -f "$DEST" ]; then
            echo "❌ Failed to copy env file to: $DEST"
            exit 1
          fi

          echo "✅ Env file copied successfully ($(wc -l < "$DEST") lines)"

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/api/Dockerfile
          push: false
          load: true
          tags: |
            ${{ env.API_IMAGE_NAME }}:${{ steps.version.outputs.version }}
            ${{ env.API_IMAGE_NAME }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Stop existing container (if running)
        run: |
          if docker ps -a --format '{{.Names}}' | grep -q "^api$"; then
            echo "Stopping and removing existing container: api"
            docker stop api || true
            docker rm api || true
          fi

      - name: Cleanup env file
        if: always()
        run: |
          rm -f ./apps/api/.env.production

      - name: Run container
        run: |
          docker run -d \
            --network=docker_my-app-network \
            --name api \
            --restart unless-stopped \
            -p 5051:5051 \
            -v ~/logs:/app/logs \
            ${{ env.API_IMAGE_NAME }}:latest

      - name: Cleanup old images
        run: |
          # 최신 2개 이미지 유지, 나머지 삭제 (생성 시간 기준)
          docker images "${{ env.API_IMAGE_NAME }}" --format "{{.ID}} {{.CreatedAt}}" | \
            sort -k2 -r | \
            tail -n +3 | \
            awk '{print $1}' | \
            xargs -r docker rmi || true

      - name: Verify deployment
        run: |
          echo "Waiting for container to be healthy..."
          sleep 5

          if docker ps --filter "name=api" --filter "status=running" | grep -q api; then
            echo "✅ Container api is running successfully!"
            docker logs --tail 20 api
          else
            echo "❌ Container failed to start"
            docker logs api
            exit 1
          fi
